buildCommand: npm install && npm run build -- --configuration production
publish: dist/angular-signup-verification-boilerplate/browser
routes:
  - type: rewrite
    source: /.*
    destination: /index.html
